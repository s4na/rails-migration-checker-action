# 要求事項

## 背景
Rails プロジェクトでは `db/migrate/*.rb` (マイグレーション) と `db/schema.rb` (スキーマスナップショット) の両方をリポジトリ管理する。両者がズレると、CI が通っていてもデプロイ時に意図しない差分や事故が起きる。

PR でマイグレーションを追加したのに `schema.rb` の更新を忘れた、あるいは手で `schema.rb` を編集してしまった、といったミスを GitHub Actions で機械的に検知したい。

## やりたいこと
GitHub Action を作成する。Pull Request でマイグレーションファイルが追加・変更されたときに、次を行う:

1. PR のベースブランチ (main または master) の `schema.rb` をチェックアウトし、まっさらな DB に対して `bin/rails db:schema:load` する。
2. PR ブランチのマイグレーションを取り込み、`bin/rails db:migrate` を実行する。
3. その結果 DB から再ダンプした `schema.rb` と、PR ブランチにコミットされている `schema.rb` の差分を比較する。
4. 差分があれば:
   - GitHub Actions のジョブを失敗させる。
   - PR 本文へのコメントとして差分内容を投稿する。
   - 差分の原因がどのマイグレーションファイルか特定できる場合、そのファイルに対して可能なら**行単位**でレビューコメントを付ける。
   - 行を特定できないが原因ファイルは推定できる場合、ファイル単位でコメントを付ける。
   - 複数行/複数ファイルなら複数コメントに分ける。
5. 差分が無ければ成功で終了。

## 受け入れ基準
- マイグレーションを追加して `schema.rb` を更新し忘れた PR で Action が失敗すること。
- マイグレーションと整合した `schema.rb` を更新済みの PR は成功すること。
- PR コメントが分かりやすく、何のテーブル/カラムが食い違っているかが読めること。
- アクションが他の Rails プロジェクトに再利用できる形 (composite action) で配布できること。
